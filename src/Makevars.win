RWINLIB = ../windows/rwinlib-1.5.2

PKG_CFLAGS=-DNNG_STATIC_LIB -I${RWINLIB}/include
PKG_LIBS=-L${RWINLIB}/lib${R_ARCH}${CRT} -lkiwi

all: winlibs

winlibs:
	"${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" "../tools/winlibs.R"

